#ifndef _MERGE3_H
#define _MERGE3_H

#include <helpers.h>

// Algoritmo de merge de 3 vias com função de comparação definida
void merge3With(elem_t *start, elem_t *mid1, elem_t *mid2, elem_t *end, comp_t comp);

#endif